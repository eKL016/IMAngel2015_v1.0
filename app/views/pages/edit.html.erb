<div class="container" style="margin-top:5%">
  <div class="row">
    <div class="md-col-4"></div>
    <div class="md-col-4">
      <div class="jumbotron">
        <%= simple_form_for @page do |f| %>
          <div class="form-group">
            <%= f.input :score, label:'對於任務打算評幾分？', input_html: { class: "form-control"} %>
          </div>
          <%= f.submit "送出", class: "btn btn-primary", data: { disable_with: "送出中..." } %>
        <% end %>
      </div>
    </div>
    <div class="md-col-4"></div>
  </div>
</div>